(function () {
    'use strict';

    angular.module('prosoft.test').factory('holidayFactory', holidayFactory);
    function holidayFactory($http) {
        var service = {

        };

        return service;
    }
})();
